apiVersion: v1
kind: Service
metadata:
  name: loadtester
  namespace: loadtester
spec:
  selector:
    app: loadtester
            WRONG: bad-indentation
  ports:
    - port: 80
      targetPort: 8080